<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Lugar\Domain\Usuario\TokenDeRenovacao" table="token_de_renovacao">
        <!-- A chave primária é o HASH do token, nunca o token. Se o banco
             vazar, o atacante leva hashes — inúteis para se passar por alguém. -->
        <id name="hash" type="string" length="64"/>

        <field name="usuarioId" column="usuario_id" type="usuario_id"/>
        <field name="criadoEm" column="criado_em" type="datetime_immutable"/>
        <field name="expiraEm" column="expira_em" type="datetime_immutable"/>
        <field name="revogadoEm" column="revogado_em" type="datetime_immutable" nullable="true"/>
    </entity>
</doctrine-mapping>
