<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Lugar\Domain\Ingresso\Ingresso" table="ingresso">
        <unique-constraints>
            <!-- RN-09: o código é a credencial de entrada. Dois iguais seria
                 duas pessoas com direito ao mesmo lugar. -->
            <unique-constraint name="uniq_ingresso_codigo" columns="codigo"/>
        </unique-constraints>

        <id name="id" type="ingresso_id"/>
        <field name="reservaId" column="reserva_id" type="reserva_id"/>
        <field name="codigo" type="codigo_ingresso"/>
        <field name="emitidoEm" column="emitido_em" type="datetime_immutable"/>
        <field name="status" type="string" length="20" enum-type="Lugar\Domain\Ingresso\StatusDoIngresso"/>
        <field name="utilizadoEm" column="utilizado_em" type="datetime_immutable" nullable="true"/>
    </entity>
</doctrine-mapping>
