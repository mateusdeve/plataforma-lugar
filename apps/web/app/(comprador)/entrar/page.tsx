import { FormularioDeAcesso } from "@/components/formulario-de-acesso";

export const metadata = { title: "Entrar — lugar." };

export default function Entrar() {
  return <FormularioDeAcesso modo="entrar" />;
}
